# Документация проекта для ИИ

## Общая информация о проекте

**Название проекта:** Ball Game  
**Тип проекта:** 3D игра на React Three Fiber  
**Среда разработки:** Vite + React  
**Деплой:** GitHub Pages через GitHub Actions

## Описание проекта

Игра про шарик, которым за время надо пройти уровень. Проект включает:
- Встроенные уровни
- Редактор уровней
- Возможность создания и сохранения своих уровней
- Главное меню с навигацией

## Технологический стек

- **React 18.2** - UI библиотека
- **Vite 5.0** - Сборщик и dev-сервер
- **React Three Fiber 8.15** - React рендерер для Three.js
- **@react-three/drei 9.88** - Полезные компоненты для R3F
- **Three.js 0.158** - 3D библиотека

## Структура проекта

```
/workspace
├── src/
│   ├── components/
│   │   ├── MainMenu.jsx      # Главное меню с кнопками
│   │   └── MainMenu.css      # Стили главного меню
│   ├── App.jsx               # Главный компонент с Canvas
│   ├── App.css               # Стили приложения
│   ├── main.jsx              # Точка входа
│   └── index.css             # Глобальные стили
├── .github/
│   └── workflows/
│       └── deploy.yml        # GitHub Actions для деплоя
├── package.json
├── vite.config.js
└── index.html
```

## Список функций

### ✅ Реализовано

1. **Главное меню (MainMenu)**
   - Компонент: `src/components/MainMenu.jsx`
   - Описание: Отображает главное меню с тремя кнопками
   - Функциональность:
     - Кнопка "Play" - для начала игры (мок)
     - Кнопка "Editor" - для открытия редактора уровней (мок)
     - Кнопка "Settings" - для настроек (мок)
     - Hover эффекты на кнопках
     - Адаптивный дизайн для мобильных и десктопов
   - Детали реализации:
     - Использует React hooks (useState) для отслеживания hover состояния
     - CSS с градиентами и анимациями
     - Media queries для адаптивности (768px, 480px breakpoints)
     - Оптимизация для touch устройств (min-height: 48px)

2. **React Three Fiber Canvas**
   - Компонент: `src/App.jsx`
   - Описание: Настроенный Canvas для 3D рендеринга
   - Конфигурация:
     - Camera position: [0, 0, 5], FOV: 75
     - Antialiasing включен
     - Базовое освещение (ambient + point light)

3. **GitHub Actions деплой**
   - Файл: `.github/workflows/deploy.yml`
   - Описание: Автоматический деплой на GitHub Pages
   - Триггеры: push в master/main, manual dispatch
   - Процесс:
     - Checkout кода
     - Setup Node.js 20
     - Установка зависимостей (npm ci)
     - Сборка проекта (npm run build)
     - Деплой на GitHub Pages

### 🚧 Планируется (не реализовано)

1. **Игровой процесс**
   - Управление шариком
   - Физика движения
   - Система уровней
   - Таймер
   - Проверка прохождения уровня

2. **Редактор уровней**
   - Создание препятствий
   - Размещение объектов
   - Сохранение уровней
   - Загрузка уровней

3. **Система сохранений**
   - Локальное хранилище
   - Экспорт/импорт уровней
   - Сохранение прогресса

4. **Настройки**
   - Графические настройки
   - Звук
   - Управление

## Детали для ИИ

### Важные константы и настройки

- **Canvas настройки:**
  - Camera position: `[0, 0, 5]`
  - FOV: `75`
  - Antialiasing: `true`

- **Breakpoints для адаптивности:**
  - Desktop: `> 768px`
  - Tablet: `≤ 768px`
  - Mobile: `≤ 480px`

- **Цветовая схема:**
  - Основной градиент кнопок: `#667eea` → `#764ba2`
  - Hover градиент: `#764ba2` → `#667eea`
  - Текст: `#ffffff`

### Паттерны кода

- Используются функциональные компоненты с хуками
- CSS модули для стилизации компонентов
- Адаптивный дизайн через media queries
- Touch-friendly интерфейс (min-height: 48px для кнопок)

### Команды разработки

- `npm run dev` - запуск dev-сервера
- `npm run build` - сборка для продакшена
- `npm run preview` - предпросмотр собранного проекта
- `npm run lint` - проверка кода линтером

### Настройка деплоя

Для работы GitHub Pages деплоя необходимо:
1. В настройках репозитория включить GitHub Pages
2. Выбрать источник: "GitHub Actions"
3. Workflow автоматически задеплоит при push в master/main

### Известные ограничения

- Кнопки меню пока только логируют в консоль (моки)
- Нет роутинга между экранами
- Нет игровой логики
- Нет редактора уровней

## Следующие шаги разработки

1. Добавить роутинг (React Router)
2. Реализовать экран игры с шариком
3. Добавить физику (cannon.js или rapier)
4. Создать систему уровней
5. Реализовать редактор уровней
6. Добавить систему сохранений

## Примечания

- Проект настроен для работы с GitHub Pages
- Base path в vite.config.js установлен как `/`
- Все зависимости актуальны на момент создания проекта
- ESLint настроен для React проектов
